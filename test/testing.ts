import { expect } from 'chai';
import {} from 'jasmine';

/* Example test */
describe('1+1 test', () => {
  it('should return 2', () => {
    const result = 1 + 1;
    expect(result).to.equal(2);
  });
});
